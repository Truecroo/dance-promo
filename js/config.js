// ===== Конфиг игры — всё, что меняется под ивент, держим здесь =====
const CONFIG = {
  // Серверный лидерборд (Supabase). Пусто -> работает локально (localStorage).
  // Как заполнить — см. BACKEND.md.
  supabase: {
    url: '',      // https://xxxx.supabase.co
    anonKey: '',  // публичный anon-ключ (безопасно хранить в клиенте при включённом RLS)
  },

  // Ивент-обвязка (брендинг/CTA/QR) — плейсхолдеры под ваш ивент
  branding: {
    qrCaption: 'Сканируй и играй с телефона',
    cta: 'Покажи этот экран на стойке — заберёшь приз 🎁',
  },

  // Геометрия поля
  canvas: { size: 480 },
  target: { radius: 104 },    // радиус центральной зоны попадания (в px)
  arrow:  { size: 54, speed: 200 }, // px в секунду к центру

  hitWindow: 34,  // допуск по расстоянию до центра для засчёта (px)

  // Длительность одного «трека» (мс). Заменим на длину реальной музыки.
  trackDuration: 60000,

  // Ритм: спавн стрелок завязан на бит. BPM заменим на BPM реального трека.
  beat: {
    bpm: 120,
    // Плотность спавна по фазам трека (доля времени 0..1 -> сколько долей на спавн).
    // every: 2 — спавн на каждый 2-й полубит; 1 — каждый полубит (плотно).
    // holdChance — вероятность hold-ноты вместо обычной в этой фазе
    phases: [
      { until: 0.25, everyHalfBeats: 4 }, // разогрев — каждые 2 бита
      { until: 0.55, everyHalfBeats: 2, holdChance: 0.12 }, // на каждый бит + холды
      { until: 0.85, everyHalfBeats: 1, holdChance: 0.15 }, // + офф-биты
      { until: 1.01, everyHalfBeats: 1, holdChance: 0.15 }, // финал — плотно
    ],
  },

  // Типы нот
  notes: {
    holdLen: 110,      // длина «хвоста» hold-ноты (px)
    holdBonus: 2.0,    // множитель очков за успешный hold
  },

  // Hype (кураж толпы). Без жёсткого провала: на 0 — «провал куража» (slump),
  // комбо рвётся и очки режутся, пока хайп не восстановится. Раунд кончается
  // только по времени.
  hype: {
    start: 60, max: 100,
    missCost: 14, gain: 4, perfectGain: 7,
    slumpMult: 0.5,   // множитель очков во время slump
    recoverAt: 30,    // хайп, при котором выходим из slump
    feverAt: 100,     // хайп для запуска Fever Mode
    feverMs: 8000,    // длительность Fever Mode
    feverMult: 2,     // множитель очков в Fever
    feverResetHype: 45, // хайп после окончания Fever (чтобы не зациклить)
  },

  // Тайминг-оценки попадания (по |d - radius|, в порядке от точного к грубому)
  timing: {
    perfect: { window: 14, mult: 1.0,  gain: 7 },
    good:    { window: 26, mult: 0.7,  gain: 4 },
    edge:    { window: 34, mult: 0.4,  gain: 2 }, // Early/Late — на краю окна
  },

  // Туториал-разминка: одиночные стрелки, без хайпа/провала/таймера.
  tutorial: {
    speedMult: 0.62,     // медленнее обычного — легче поймать
    mercyMisses: 2,      // после стольких промахов по стрелке — пропускаем дальше
    steps: [
      { text: 'Жми кнопку той стороны, откуда летит стрелка — когда она в кольце',
        arrows: ['up', 'right', 'down', 'left'] },
      { text: 'Связка! Поймай ритм: ← → ← →',
        arrows: ['left', 'right', 'left', 'right'] },
    ],
    groove: 'Nice! Поймал грув 🕺',
  },

  // Тексты экрана Select (вход в зачётный режим)
  select: {
    title: 'Тебя вызвали в Select!',
    text: 'Разминка пройдена. Тот же трек — но толпа жёстче, а стрелки плотнее. Выходи на сцену и бери топ.',
    track: 'CLUB GROOVE',
  },

  // Ранги по точности (accuracy %). Первый подходящий сверху.
  ranks: [
    { min: 95, rank: 'S' },
    { min: 85, rank: 'A' },
    { min: 70, rank: 'B' },
    { min: 50, rank: 'C' },
    { min: 0,  rank: 'D' },
  ],

  // Очки в зависимости от текущего комбо
  scoreTiers: [
    { combo: 10,  points: 50 },
    { combo: 25,  points: 75 },
    { combo: 50,  points: 100 },
    { combo: 100, points: 150 },
    { combo: Infinity, points: 200 },
  ],

  // Настроение танцора по уровню комбо (порог -> класс)
  moods: [
    { combo: 0,  mood: 'idle'  },
    { combo: 1,  mood: 'warm'  },
    { combo: 8,  mood: 'groove'},
    { combo: 20, mood: 'fire'  },
    { combo: 40, mood: 'star'  },
  ],

  // Спрайт-анимации танцора (halo pack). frames — число кадров, fps — скорость цикла.
  dancer: {
    basePath: 'assets/dancer',
    // w — ширина отображения (px). Подобрана так, чтобы рост персонажа был
    // одинаковым во всех настроениях (кадры обрезаны под контент по-разному).
    sprites: {
      idle:   { frames: 6,  fps: 3,  w: 157 },
      warm:   { frames: 6,  fps: 4,  w: 143 },
      groove: { frames: 8,  fps: 6,  w: 158 },
      fire:   { frames: 8,  fps: 8,  w: 162 },
      star:   { frames: 10, fps: 10, w: 168 },
      miss:   { frames: 4,  fps: 8,  w: 156 },
    },
  },

  // Цвета стрелок по направлению (плейсхолдер вместо картинок)
  dirColors: {
    up:    '#ff5a8a',
    down:  '#4ad6ff',
    left:  '#ffd23f',
    right: '#7c5cff',
  },

  // Клавиатура -> направление
  keyMap: {
    ArrowUp: 'up', ArrowDown: 'down', ArrowLeft: 'left', ArrowRight: 'right',
  },

  // Финальные тексты по результату (порог очков -> текст)
  endings: [
    { min: 0,    title: 'НУ... БЫЛО',     story: 'Герой потоптался и ушёл за лимонадом. Зато попробовал!' },
    { min: 1500, title: 'ОГОНЬ!',         story: 'Толпа завелась, кто-то снял сторис. Неплохой выход!' },
    { min: 4000, title: 'ЗВЕЗДА ВЕЧЕРА',  story: 'Диджей дал отдельный трек. Герой ушёл под овации.' },
  ],
};
